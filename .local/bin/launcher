#!/bin/bash  

rofi -modi 'drun,window' \
    -matching fuzzy \
    -drun-match-fields name \
    -config $XDG_CONFIG_HOME/rofi/rofi.conf \
    -theme $XDG_CONFIG_HOME/rofi/themes/merged.rasi \
    -show drun
